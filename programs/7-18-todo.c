#include <stdio.h>

float f(float a3, float a2, float a1, float a0, float x);

int main() {
    float a3, a2, a1, a0;
    scanf("%f %f %f %f", a3, a2, a1, a0);
    float start, end;
    scanf ("%f %f", start, end);


}

float find_solution(float a3, float a2, float a1, float a0, float start, float end) {
}

float f(float a3, float a2, float a1, float a0, float x) {
    return a3 * x * x * + a2 * x * x + a1 * x  + a0;
}
